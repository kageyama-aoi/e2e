# HANDOFF - 2026-06-15

## 使用ツール
Claude Code (claude-sonnet-4-6)

## 現在のタスクと進捗

- [x] docs/shimamura/ 配下2ファイルを現状に合わせて更新 (#134)
  - skill_plan.md: pages/shimamura/ 再編・テスト23件・loadCsvWithProfile対応を反映
  - syokai_flow_page_guide.md: require パス・ファイル構成図を flow/ 移動後に合わせて修正
- [x] shimamura スキル2件のパスを更新 (#135)
  - shimamura-ichiran-dev/SKILL.md: tests/shimamura/page/, require ../../../, CSV dataDir明示
  - shimamura-registration-dev/SKILL.md: tests/shimamura/flow/, pages/shimamura/flow/, require ../../../
- [x] スキル管理場所の検討・検証
  - ~/.claude/skills/ (グローバル) vs .claude/commands/ (プロジェクト) の比較を実施
  - doc-sync を .claude/commands/doc-sync.md として移行テスト → system-reminderに検出された（動作確認OK）

## 試したこと・結果

- **doc-sync のプロジェクトコマンド化**: `.claude/commands/doc-sync.md` を作成（フロントマターなし・本文のみ）
  → 新セッションの system-reminder に `doc-sync` が2件表示（グローバル版 + プロジェクト版）→ 検出成功
- **スキルフォーマットの差異確認**: `~/.claude/skills/` はフロントマターあり、`.claude/commands/` はフロントマターなし

## 次のセッションで最初にやること

1. **スキル全件移行の実行**（doc-sync の動作確認が取れたため）
   - 移行対象: shimamura-html-fetch, shimamura-ichiran-dev, shimamura-registration-dev, tframe-html-fetch, tframe-ichiran-dev, tframe-registration-dev, doc-sync
   - 手順: 各 SKILL.md のフロントマター（--- で囲まれた部分）を除いた本文を `.claude/commands/{name}.md` として作成
   - グローバル側の対応スキルフォルダは削除する
   - github-issue-dev / safe-move / string-template-refactor は ~/.claude/skills/ に残す

2. **AGENTS.md のスキル一覧を更新**（スキルの所在が変わったため doc-sync カテゴリ参照元を変更）

3. **_temp_review_汎用性.md の削除**（全対応済み、作業メモとして役目を終えた）

4. **⑪ 新サイトオンボーディング手順書の作成**（docs/ のどこに置くかも含めて検討）

## 注意点・ブロッカー

- doc-sync が system-reminder に**2件**表示されている（グローバル版と プロジェクト版の重複）
  → 全件移行完了後にグローバル版（~/.claude/skills/doc-sync/）を削除すれば解消する
- スキルの invocation は `/doc-sync` のまま変わらず使えることを確認済み（新セッションで試すこと）
- `~/.claude/skills/doc-sync/SKILL.md` はまだ残っているため移行完了後に削除する
