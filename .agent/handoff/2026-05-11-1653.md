# HANDOFF - 2026-05-11 17:00

## 使用ツール
Claude Code (claude-sonnet-4-6)

## 現在のタスクと進捗
- [x] #74: data/tframe/README.md SideMenu 対応表の修正・doc-sync・README.md 最新化
- [x] #75: IchiranMixin 作成（12 Page Object から clickSearchAndWait 等3メソッドの重複除去）
- [x] #76: isEnglish() インライン実装・ラッパーメソッドを utils.js の require に統一（10ファイル）
- [x] #77: CalendarPage / HomePage / KoshiPage / JukuseiPage の Mixin 上書きデッドコード削除（4ファイル）
- [x] #78: CoursePage / ReportPage / MasterMenuPage / KeiryoMasterPage / EmailPage / HelpPage の Mixin 上書きデッドコード削除（6ファイル）
- [ ] #2（優先度高）: fillAddressInfo / fillMemoInfo の共通化（utils.js に関数追加）— まだ未着手

## 試したこと・結果

### 成功したアプローチ
- **IchiranMixin（#75）**: `createIchiranMixin(screenLabel)` ファクトリ関数を `pages/tframe/_common/IchiranMixin.js` に新規作成し、12ファイルの3メソッド（計約300行）を `...createIchiranMixin('画面名')` 1行に置き換え
- **isEnglish 統一（#76）**: パターン③（インライン実装）8ファイルは `require` 追加 + メソッド削除 + `this.isEnglish()` → `isEnglish()` 変更。パターン②（ラッパー）1ファイル（KoshiPage）はメソッド削除のみ
- **デッドコード削除（#77/#78）**: JS オブジェクトスプレッドの「後書きが勝つ」ルールにより Mixin に上書きされているメソッドを合計10ファイルから削除（合計約880行削除）。EmailPage は固有メソッド（`verifyMenuStructure` / `seeGroup` 等）を保持しつつ推移的デッドコードのみ除去

### 詳細解説ドキュメント
- `docs/design/refactoring-tframe-3-4-detail.md` を新規作成（#3・#4 の JavaScript 基礎から段階的に解説）
- `docs/design/refactoring-tframe-high-priority.md` に全4件の解説あり

## 次のセッションで最初にやること
1. **#2（fillAddressInfo / fillMemoInfo の共通化）** に着手する場合：
   - 対象ファイルの `fillAddressInfo` を全件 grep して実装の差異を確認（独自フィールドを含むファイルがないか）
   - `support/utils.js` に `fillAddressWithZip(I, data, label)` / `fillMemoField(I, data, label)` を追加
   - 各 Page Object の require に追加 → 既存メソッドを置き換え
2. 着手前に `docs/design/refactoring-tframe-high-priority.md` の #2 解説（注意点の項）を必ず確認する

## 注意点・ブロッカー
- **CoursePage の `scrollToItem`**: `scrollToHref` 定義は削除済みだが `scrollToItem` 内で `this.scrollToHref()` を呼んでいる。実行時は Mixin 版が使われるため動作上の問題はないが、`scrollToItem` 自体もデッドコードの可能性がある（Mixin の `clickMenuItemAndVerify` から呼ばれていない）。次セッションで確認・削除を検討してよい
- **#2 の注意**: `fillAddressInfo` は JukuseiPage だけガード条件（`if (!data.primaryAddressPostalcode && !data.primaryAddressStreet) return;`）が追加されており、単純な共通化では対応できない。実装前に全ファイルを読み比べること
- **コミット方針**: ステージングは作業ファイルのみ（`git add` で個別指定）。`git add .` は使わない
