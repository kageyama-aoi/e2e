# HANDOFF - 2026-04-28 17:30

## 使用ツール
Claude Code (Sonnet 4.6)

## 現在のタスクと進捗
- [x] Stop hook — ハンドオフ自動リマインダー（前回完了）
- [x] PostToolUse(Write) hook — ファイル配置バリデーション（前回完了）
- [x] PostToolUse(Bash) hook — テスト実行後の Allure 自動アーカイブ（今回完了）

## 今回の変更内容
- `scripts/hooks/archive_allure.py` 新規作成
  - `codeceptjs run` / `npm run test_*` / `npm test` / `npx codeceptjs` を検知
  - 成否問わず `npm run allure:archive` を自動実行
- `.claude/settings.local.json` に `PostToolUse(Bash)` matcher を追加
- `AGENTS.md` の `scripts/` サブフォルダ表に `hooks/` 行を追記

## ユーザーがやること（次回以降）
- [ ] **動作確認**: tframe などのテストを1本実行し、ステータスバーに「Allure アーカイブ中...」が表示されることを確認する
- [ ] **確認後**: 問題なければこのフックは本稼働。問題があれば `scripts/hooks/archive_allure.py` のパターン or コマンドを調整する

## 注意点
- settings.local.json の `permissions.allow` に過去セッションのテストコマンド残骸が蓄積中（整理は任意）
