# HANDOFF - 2026-05-07

## 使用ツール
Claude Code (claude-sonnet-4-6)

## 現在のタスクと進捗
- [x] tframe コース登録テストを追加 #64（PASS・コミット済み）
- [x] tframe 受講生登録テストを追加 #65（PASS・コミット済み）
- [ ] 校舎登録テスト（masterSideMenu 配下）：未着手
- [ ] 教室登録テスト（masterSideMenu 配下）：未着手
- [ ] 料金マスタ作成テスト（accountingSideMenu 配下）：未着手
- [ ] 料金パッケージ作成テスト（accountingSideMenu 配下）：未着手

## 試したこと・結果

### 成功したアプローチ

**コース登録テスト（#64）**
- `fetch_tframe_forms.js` に `directUrl` プロパティを追加してリンク検索なしで直接URLアクセスできるよう拡張
- `CoursePage.js` にメソッドを追加：`navigateToRegisterPage` / `fillCourseBasicInfo` / `fillMemoInfo` / `submitAndVerifyRegistration`
- `school_area_id` → `school_branch_id` AJAX連動に `I.wait(1)` を挿入して対応
- culture_beta プロファイルで 2/2 PASS

**受講生登録テスト（#65）**
- `JukuseiPage.js` にメソッドを追加：`navigateToRegisterPage` / `fillPersonalInfo1` / `fillPersonalInfo2` / `fillAddressInfo` / `fillMemoInfo` / `submitAndVerifyRegistration`
- 郵便番号ボタン（`#zipCodeBtn`）で都道府県・市区町村を自動入力（番地・カナは手動入力）
- `school_area_id` → `school_branch_id` AJAX連動対応済み
- 保存後の確認テキストは `current.lastName`（受講生詳細ページに姓が表示される）
- culture_beta プロファイルで 2/2 PASS

### メモ
- `fetch_tframe_forms.js` の `directUrl` 対応により、今後の HTML 取得は TARGETS に URL を直書きするだけでよい
- 受講生登録は銀行情報・学年・媒体など任意フィールドが多いが、今回は最小限（姓・名・ふりがな・電話・性別・生年月日・メール・ID・ステイタス・校舎・入学日・住所・メモ）に絞った

## 次のセッションで最初にやること
1. 校舎登録テストの HTML を `fetch_tframe_forms.js` で取得する
   ```
   { name: 'branch_touroku', hint: '校舎登録', directUrl: `${BASE_URL}index.php?r=branch%2Few%2F_default` }
   ```
2. `tframe_extract_form_fields.js` でフィールドを抽出する
3. `MasterMenuPage.js` に登録メソッドを追加するか、別 Page Object を作成するか判断する
   - `masterSideMenu` 配下なので `MasterMenuPage.js` に追加するパターン、または `BranchPage.js` を新規作成するパターンが考えられる
4. 同様の流れで教室登録テストも続けて実装

## 注意点・ブロッカー
- 校舎登録・教室登録は `masterSideMenu` 配下のため、Page Object を `MasterMenuPage.js` に相乗りするか新規作成するかを `/placement-gate` で確認してから実装すること
- 料金マスタ・料金パッケージは `accountingSideMenu` 配下。テストデータ（料金設定）が複雑になる可能性があるため、後回しでよい
- culture_beta 環境で使用可能な校舎は `school_branch_id = 5f8be3c0-c4b0-bc31-b9fd-661dcbb3beba`（ダミー校舎）
- `personStatus` の選択肢は culture_beta では 1（問合せ）/ 2（トライアル）/ 4（休会）/ 5（旧受講生）のみ（在籍中なし）
