# HANDOFF - 2026-04-23

## 使用ツール
Claude Code

## 現在のタスクと進捗

- [x] `data/tframe/` の使用状況調査（全18ファイルの参照元を確認）
- [x] `data/tframe/README.md` 作成（テスト↔CSV対応表・minimum.csvの設計方針・SideMenu対応表を記載）
- [x] SideMenu ファイル（9本）と `_urlPath.js` を `data/tframe/` → `pages/tframe/` へ移動
- [x] 移動に伴う require パス更新（11テストファイル）
- [x] AGENTS.md にディレクトリ配置ルール（否定ルール・新ファイル作成時の分類宣言・新概念フロー）を追記
- [x] AGENTS.md にファイル移動プロトコル・ドキュメント連動ルール・スキル一覧を追記
- [x] `/placement-gate` スキルを新規作成（`.claude/commands/placement-gate.md`）
- [x] `docs/guides/project_architecture_guide.md` を最新構成に更新
- [x] プロジェクト全体の構造レビュー（残課題3点を洗い出し）
- [ ] `docs/generated/` を .gitignore に追加して git 追跡を外す（中優先）
- [ ] `LoginMyPageStudent.js` / `LoginMyPageTeacher.js` のリネーム検討（高優先）
- [ ] `data/tframe/teacherRegisterData.js` の削除（低優先）

## 試したこと・結果

### 成功したアプローチ
- `git mv` で SideMenu ファイルを移動（git 上で RM として追跡される）
  - ただし `_urlPath.js` が未追跡だったため `git mv` が使えず、`mv` + 後から `git add` の手順に
- `grep -r "data/tframe/.*SideMenu"` で旧パス残留ゼロを確認してから完了とした
- AGENTS.md に「置いてはいけないもの」の否定ルールを追加したことで配置ルールが機能する形になった

### 設計上の決定事項
- `data/tframe/_minimum.csv` の扱い：削除ではなく「必須フィールドのみのテストシナリオ用」として保持
  - テスト実装時は `Data(csvFull)` と `Data(csvMinimum)` を別ブロックで定義し `@full` / `@minimum` タグで分ける方針
  - 現状 minimum.csv の列が不完全（2列のみ）なので、実装前に必須フィールドの洗い出しが必要
- `teacherRegisterData.js`：JS関数でデータ生成する旧方式の名残。CSV方式に移行済みで参照なし → 削除候補として README に記載

## 次のセッションで最初にやること

1. `docs/generated/` を `.gitignore` に追加し `git rm --cached -r docs/generated/` で git 追跡を外す
2. `LoginMyPageStudent.js` / `LoginMyPageTeacher.js` のリネーム影響範囲を確認（require パスの grep）
3. 上記2点が完了したらコミット（`chore: docs/generated を gitignore に追加、LoginMyPage系ファイルをリネーム`）

## 注意点・ブロッカー

- `data/tframe/teacherRegisterData.js` は `KoshiPage.js` の JSDoc コメント（L191）にのみ出現。削除するときはコメントも合わせて修正すること
- `_minimum.csv` の実テスト組込みは「必須フィールド洗い出し」が前提。フォームの HTML を確認するか、バリデーションエラーをわざと出して必須項目を特定する手順が必要
- SideMenu ファイル内の `require('./_urlPath')` パスは両ファイルが同じ `pages/tframe/` にいるため変更不要（移動後も正しく動作する）
- `emailSideMenu.js` の「連絡一覧」エントリが `href` 未定義のまま（culture 環境固有の URL が不明）。`MenuNavigationMixin.js` に href guard を追加済みなのでテストは落ちないが、正しい URL は要調査
