# HANDOFF - 2026-05-08 12:30

## 使用ツール
Claude Code (claude-sonnet-4-6)

## 現在のタスクと進捗
- [x] PostToolUse フック `scripts/hooks/log_bash.py` 実装・コミット (#73)
- [x] AGENTS.md メンテナンス（`*SideMenu.js` → `sideMenus.js`、hooks 説明更新）
- [x] tframe 未テスト画面の分析 → `docs/design/tframe_screen_coverage.md` に保存

## 試したこと・結果
- **log_bash.py**: PostToolUse フックで stdin の JSON を受け取り `docs/learning/bash_YYYYMMDD.md` に追記 → 実装直後から記録されていることを確認済み
- **run_gui.py 拡張**: `_cleanup_old_learning_logs` 関数を追加し、起動時に `docs/learning/bash_*.md` の30日以上古いファイルを自動削除
- **AGENTS.md**: `pages/` の配置ルール（`*SideMenu.js` → `sideMenus.js`）と `scripts/hooks/` の説明を修正 → コミット済み
- **画面カバレッジ分析**: `sideMenus.js` 全 URL と `tests/tframe/` を照合し、未テスト画面を系統別に整理

## 次のセッションで最初にやること
1. `docs/design/tframe_screen_coverage.md` を参照して、次にテストを作る画面を選ぶ
   - 難易度「低」候補：複合一覧（コース別受講生一覧 `student/sw/stByCourse` など）、対応履歴系
   - 難易度「中」候補：出席・入退室、Eメールテンプレート系、レポート系
2. 選んだ画面に対して `/tframe-ichiran-dev` または `/tframe-registration-dev` スキルで開発
3. `data/tframe/README.md` の SideMenu 対応表を更新（旧 `*SideMenu.js` ファイル名が残っている）

## 注意点・ブロッカー
- `docs/learning/bash_*.md` は `.gitignore` 追加済みで git 管理外
- 経理系・一括処理系・インポート/エクスポート系は副作用リスクが高く、テスト設計は別途要検討
- Eメールの「連絡一覧」は `sideMenus.js` に `href` がなく（`altName` のみ）、URL が不明 → 実際の画面を確認してから設計する必要がある
- 前セッションからの未コミット変更が多数残っている（`pages/tframe/MenuNavigationMixin.js`, `tests/tframe/` 各ファイル, `support/envLoader.js` など）— 別タスクの変更の可能性があるため確認が必要
