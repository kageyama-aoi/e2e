# HANDOFF - 2026-05-07 16:xx

## 使用ツール
Claude Code (claude-sonnet-4-6)

## 現在のタスクと進捗
- [x] tframe 登録テスト6画面（コース・受講生・教室・料金マスタ・校舎・料金パッケージ）→ 前セッションで完了
- [x] tframe 一覧検索テスト B+C パターン設計・方針決定
- [x] 受講生一覧・コース一覧テスト実装（#70）
- [x] 残り9画面の一覧検索テスト実装（講師/商品/調整金/アカウント/スタッフ/教室/校舎/料金マスタ/料金パッケージ）（#71）

## 試したこと・結果

### 成功したアプローチ
- **B+C パターン**: CSV の `expectedName` が空なら `verifyResultsExist()`、値があれば `verifyRecordInResults()` を呼ぶ単一 Data シナリオで実現。テストファイル側の if 分岐でシンプルに実装
- **`fetch_tframe_forms.js` に directUrl 追加**: リンク検索をスキップして直接URLでHTMLを取得できるように拡張。一覧画面（SW）URLを9画面分追加して一括取得
- **`_fetch_juku_lists.js` 一時スクリプト**: ryokin_master_list・ryokin_package_list は culture_beta で500エラーのため juku_test プロファイルで個別取得
- **Worker エージェントによる9画面一括実装**: Page Object 9ファイル修正 + CSV 9本 + テストファイル 9本をまとめて生成。CSV列数チェックもすべてOK

### 失敗・注意点
- **調整金一覧（shareiDetail/sw）**: 検索フォームに名前系フィールドがない（keijouMonthYear/Month + popup picker 構成）。C パターンは「年度で絞り込む」に留め、`expectedName` は常に空→ `verifyResultsExist()` のみ
- **校舎一覧の `SchoolCode`**: フィールドIDが大文字S（`SchoolCode`）。HTML確認で発見・正確に反映済み

## 次のセッションで最初にやること
1. `run/run_gui.bat` でテスト実行・結果確認
   - culture_beta プロファイルで9本（登録データ未作成の場合は先に `*_touroku_test.js` を流す）
   - juku_test プロファイルで ryokin_master_ichiran / ryokin_package_ichiran の2本
2. 失敗テストがあればスクリーンショット（`output/` フォルダ）を確認して原因特定・修正
3. 一覧テストパターンをスキル化（`/tframe-list-search-dev` または既存スキルに追記）

## 注意点・ブロッカー

### 実行前提
- C パターン（条件付き検索）は対応する `*_touroku_test.js` で登録済みのデータを検索する
- 未登録環境では C パターンが `verifyResultsExist()` で結果0件タイムアウトになる
- 料金マスタ・料金パッケージは **juku_test プロファイルのみ**（culture_beta は 500 エラー）

### 調整金一覧の制約
- `chosekin_ichiran_test.js` の B パターン（空検索）は謝礼データが1件もないと `waitForElement('.tf-group-body-search-result tr', 15)` がタイムアウトする
- 環境に謝礼データがない場合は B パターンをスキップするか、B 行の `expectedName` を空にして `verifyResultsExist()` を別途確認が必要

### 実装済み一覧スキル化
- 現時点で `/tframe-list-search-dev` スキルはまだ存在しない
- 2画面実装後にスキル化する方針だったが、先に11画面実装完了
- 次セッションでスキルドキュメントを作成しておくと今後追加しやすい

### CSS カラム命名
- 商品一覧の校舎フィールドは `branchId_area_id` / `branchId_branch_id`（他画面の `school_area_id` と異なる）
- スタッフ一覧も touroku では `branchId_area_id` だが一覧（SW）では `school_area_id`（HTML 確認済み）
