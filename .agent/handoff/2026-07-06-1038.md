# HANDOFF - 2026-07-06 10:38

## 使用ツール
Claude Code（Fable 5）

## 現在のタスクと進捗

全スキルの棚卸しレビューと洗練（対象4か所・計26ファイル：e2e `.claude/skills` 11 + `.claude/commands` 3、グローバル `~/.claude/skills` 4 + `~/.claude/commands` 5、マクロ開発環境 `.agent/skills` 6）。

改善項目を4クラスタに分類し、1〜3が完了。

- [x] クラスタ3：構造改善（Issue #171、e2e コミット `55f91ae`）
  - shimamura-registration-dev を本体430行 + `references/patterns.md` + `references/troubleshooting.md` に分割。今後の落とし穴追記先は references/ 側
  - context-diet / string-template-refactor（グローバル）に frontmatter 追加（自動トリガーが効くように）
  - make_project の古いモデル名例示・番号入力方式を修正
- [x] クラスタ1：鮮度のズレ修正（Issue #172、e2e コミット `38421b6`）
  - tframe-registration-dev / tframe-ichiran-dev の参照パスを `pages/tframe/screens/` に修正（計7箇所）
  - shimamura-ichiran-dev「Phase 4 で作成予定」、shimamura-html-fetch「将来作成する」の残留を解消
  - doc-sync のカテゴリEを全プロダクト対象に更新、呼び出しタイミング表に shimamura 系3スキル追加
  - AGENTS.md のスキル一覧に /launcher-review 追記、shimamura-screen-diagram を git 管理下に追加
- [x] クラスタ2：重複・二重管理の解消（Issue #173、e2e `437d3d3` / マクロ開発環境 `0da3148`）
  - vba-review 一本化：グローバル版403行の知見を `マクロ開発環境/docs/VBA注意事項.md` に統合（B-6〜B-9、C-7〜C-12、新設D節=外部ブック操作7項目）。スキル版・コマンド版とも「目次を動的参照」方式に変更（ナレッジは1箇所直せば両方に効く）。編集前アーカイブ: `docs/archive/VBA注意事項_2026-07-06.md`
  - local-safe-move を /safe-move への委譲型にスリム化（211→130行）。グローバル safe-move に「プロジェクト専用スキル優先」の棲み分けを明記
  - make_project → make-project にリネーム（IDE診断でスキル名の命名規則違反が判明したため）。newplan/handoff テンプレートを `templates/` に切り出しコピー方式に。参照4箇所（CLAUDE.md・README.md）更新済み
  - gas-new Step 5.5 の原則再掲を gas-api-review 参照+要点4行に圧縮（153→96行）
- [ ] クラスタ4：トリガー最適化（未着手・任意）
  - skill-creator の評価ループ（should/should-not-trigger 20クエリ）で description を計測・改善する。誤発動しやすいペア（safe-move系、ichiran vs registration）が候補。時間がかかるため別セッション推奨

## 試したこと・結果

- 成功：全26ファイルを読み込み、AGENTS.md・実ファイル配置（Glob/Grep）・git状態と突き合わせて乖離を裏取りしてから修正する方式。skill-creator スキルのベストプラクティス（500行上限・progressive disclosure・pushy な description）を評価基準に使用
- 成功：ナレッジの「参照型化」（vba-review・doc-sync・gas-new）。チェックリストをスキルに書き写さず目次を動的参照させることで、二重管理を構造的に防止
- 注意：make_project リネーム時、`git mv` はサブフォルダの未追跡ファイル（templates/）も一緒に移動できた。マクロ開発環境の sed 一括置換で想定外の `業務フロー/` 配下のコピーがヒットした（下記ブロッカー参照）

## 次のセッションで最初にやること

1. ユーザーに `マクロ開発環境/業務フロー/.agent/skills/make_project/`（旧版346行のコピー・独立gitリポジトリ内）を削除してよいか確認する。A-3「.agent/skills/ は親フォルダで一元管理」ルールに反する古いコピー
2. クラスタ4（トリガー最適化）をやるか確認。やる場合は skill-creator の description 最適化ループを safe-move / local-safe-move ペアから開始
3. メモリ `project_skill_review_2026-07.md` に全経緯あり（調査の再実施は不要）

## 注意点・ブロッカー

- `業務フロー/` は make_project が生成した独立 git リポジトリのため、親リポジトリの git status には映らない。削除はユーザー判断待ち
- shimamura-registration-dev の新しい落とし穴・パターンは SKILL.md 本体ではなく `references/troubleshooting.md` / `references/patterns.md` に追記すること（本体には追記先の案内を明記済み）
- VBA注意事項.md の項目番号（B-6〜B-9、C-7〜C-12、D-1〜D-7）を増減させる場合、vba-review のスキル版・コマンド版は目次参照なので修正不要（それが狙いの設計）
- e2e の作業ツリーにはスキル以外の未コミット変更が多数残っている（今回の作業とは無関係。`git add .` は使わないこと）
